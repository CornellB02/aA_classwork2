function PeeQs(word){
    let str = ''
    let count = 0
    for (let i = 0; i < word.legnth; i++){
        let char = word[i]
        let currentCount = 0
        if (char === "P" || char === "Q"){
            count ++
        } else if (char !== "P" || char !== "Q"){
            
        }

    }

}